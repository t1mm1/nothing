{#
/**
 * @file
 * Theme override to display a 403 page.
 */
#}
<div class="min-h-screen flex flex-col">
  <main class="grid min-h-full place-items-center bg-white px-6 py-24 sm:py-32 lg:px-8">
    <div class="text-center">
      <p class="text-base font-semibold text-blue-600">Oops!</p>
      <h1 class="mt-4 text-balance text-5xl font-semibold tracking-tight text-gray-600 sm:text-7xl">403 Access Denied</h1>
      <p class="mt-6 text-pretty text-lg font-medium text-gray-600 sm:text-xl/8">You do not have permission to access this page.</p>
      <div class="mt-10 flex items-center justify-center gap-x-6">
        <a href="{{ path('<front>') }}" class="rounded-md bg-blue-600 px-6 py-3 text-sm font-semibold text-white shadow-sm hover:bg-blue-700 duration-500" title="Home">Home</a>
      </div>
    </div>
  </main>

  {# Footer #}
  {% include '@nothing/layout/footer.html.twig' %}
</div>
