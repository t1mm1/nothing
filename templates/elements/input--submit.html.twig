{#
/**
 * @file
 * Theme override for submit button.
 */
#}
{% set classes = [
  'px-6 py-3 font-semibold rounded-lg cursor-pointer bg-blue-600 text-white hover:bg-blue-700',
  'transition-colors duration-500',
  'disabled:bg-gray-400 disabled:cursor-not-allowed border-none',
] %}

<input{{ attributes.addClass(classes) }} />
