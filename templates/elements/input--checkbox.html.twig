{#
/**
 * @file
 * Theme override for checkbox input.
 */
#}
{%
  set classes = [
  'w-4 h-4 mr-2 text-blue-600 bg-white border-gray-300 rounded cursor-pointer flex-shrink-0',
]
%}

<input{{ attributes.addClass(classes) }} />
