{#
/**
 * @file
 * Theme override for checkbox input.
 */
#}
{%
  set classes = [
  'w-4 h-4 text-blue-600 bg-white border-gray-300 rounded focus:ring-2 focus:ring-blue-500 cursor-pointer flex-shrink-0',
]
%}

<input{{ attributes.addClass(classes) }} />
