{#
/**
 * @file
 * Theme override for button.
 */
#}
{% set classes = [
  'px-6 py-3 font-semibold rounded-lg cursor-pointer bg-gray-300 text-gray-600 hover:bg-gray-200 transition-colors duration-500 border-none',
] %}

<input{{ attributes.addClass(classes) }} />
