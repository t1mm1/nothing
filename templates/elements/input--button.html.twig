{#
/**
 * @file
 * Theme override for button.
 */
#}
{% set classes = [
  'px-6 py-3 font-semibold rounded-lg cursor-pointer bg-gray-300 text-gray-600 hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-600 transition-colors duration-500 border-none',
] %}

<input{{ attributes.addClass(classes) }} />
