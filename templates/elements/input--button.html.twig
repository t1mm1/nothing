{#
/**
 * @file
 * Theme override for button.
 */
#}
{% set classes = [
  'px-6 py-3 font-semibold rounded-lg cursor-pointer bg-gray-200 text-gray-900 hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-500 transition-colors duration-200 border-none',
] %}

<input{{ attributes.addClass(classes) }} />
